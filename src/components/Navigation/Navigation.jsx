import React from 'react';
import {BottomNavigation, BottomNavigationItem} from 'material-ui/BottomNavigation';

const Navigation = () => (
  <BottomNavigation>
    <BottomNavigation label='hello' />
  </BottomNavigation>
);

export default Navigation;